# Image Similarity Search using Data Structures

![Language](https://img.shields.io/badge/language-C%2B%2B-blue)
![Build](https://img.shields.io/badge/build-CMake-green)
![Library](https://img.shields.io/badge/library-OpenCV-orange)

## Project Overview

This repository contains the source code for the practical assignment of the **Algorithm Design and Analysis** course (2025/2) at the Pontifical Catholic University of Minas Gerais (PUC Minas), under the supervision of Prof. Silvio Jamil F. Guimarães.

The project's objective is to perform a theoretical and practical computational cost analysis of three data structures applied to a Content-Based Image Retrieval (CBIR) problem.The goal is to find images in a database that are visually similar to a given query image.The similarity is determined by comparing feature vectors extracted from the images.

The data structures implemented and analyzed are:
1.  **Sequential List** (baseline with linear search)
2.  **Quadtree** (generalized for N-dimensions as a **K-d Tree**)
3.  **Hashing** (using the **Locality-Sensitive Hashing - LSH** technique)

The implementation is required to be in C/C++.

## Key Features

-   **Feature Extraction:** Extracts 24-dimensional feature vectors from color images using 8-bin RGB color histograms.
-   **Data Structure Implementation:** Provides functional implementations for `DocumentList`, `KdTree`, and `DocumentHash` (LSH).
-   **Similarity Search:** Uses Euclidean distance to measure the similarity between feature vectors.
-   **Performance Analysis:** Times the search operations for each data structure in microseconds to allow for practical performance comparison.
-   **Result Visualization:** Displays the query image and the most similar result side-by-side in a graphical window for qualitative analysis.
-   **Modern Build System:** Uses CMake to manage the build process and all OpenCV dependencies.

## Project Structure

```
.
├── src/          # Source code files (.cpp) with implementations
├── include/      # Header files (.h) with declarations
├── data/         # Images for tests and experiments
├── build/        # Build files (generated by CMake)
├── bin/          # Final executable (generated by make)
├── report/       # Folder for the LaTeX report and its PDF
└── CMakeLists.txt # Build configuration file
```

## Prerequisites

To compile and run this project, you will need:
-   A C++ compiler supporting C++17 (e.g., `g++`).
-   `CMake` (version 3.10 or higher).
-   `OpenCV` (version 4.x) with the following modules: `core`, `imgproc`, `imgcodecs`, and `highgui`.

## Compilation and Execution

Follow these steps to compile and run the program from the command line:

1.  **Clone the repository:**
    ```bash
    git clone <your-repository-url>
    ```

2.  **Navigate to the project's root directory:**
    ```bash
    cd <repository-folder>
    ```

3.  **Create the build directory and configure the project with CMake:**
    ```bash
    mkdir -p build
    cd build
    cmake ..
    ```

4.  **Compile the code:**
    ```bash
    make
    ```

5.  **Run the executable:**
    Make sure you are in the project's **root directory** before running the program so that it can find the `data/` folder.
    ```bash
    # If you are inside the 'build' directory, go back to the root
    cd ..

    # Run the program
    ./bin/meu_programa
    ```

## Usage and Output

When the program is executed, it will perform the following steps:
1.  Load the dataset of images defined in `src/main.cpp` from the `data/` directory.
2.  Print the performance experiment header to the terminal.
3.  For each of the three data structures, it will print the filename of the most similar image found and the time the search took in microseconds.
4.  For each successful search, a graphical window will open displaying the query image on the left and the found result on the right.
5.  The program will wait for you to press any key while one of the image windows is active to close all windows and terminate.

## Group Members

[cite_start]As required by the assignment, this project was developed by:
-   *Member Name 1*
-   *Member Name 2*
-   *Member Name 3*
-   *Member Name 4*
-   *Member Name 5*

